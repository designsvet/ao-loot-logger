node_modules/
dist/
.idea
.tmp*

*.txt
*.gz
*.log
*.error
*.exe

# Raw packet recordings from the dump tool. They contain a guild private data —
# member names, energy amounts, guild and player ids — and must never be committed.
guild-dump-*.jsonl
loot-events-*.txt
debug-logs.txt*

# The last item table the server confirmed (src/items.js) — a download cache, rewritten at will.
items-cache.json
items-cache.json.*.tmp
